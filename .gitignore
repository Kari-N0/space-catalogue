# deps / build
node_modules/
dist/
*.tsbuildinfo

# runtime job state (ADDON.md §6)
jobs/

# heavy source assets live outside git (PLAN.md §8) — git-lfs later if needed;
# markdown (scene bibles, notes) IS tracked
assets_src/**
!assets_src/**/
!assets_src/**/*.md
pipeline/rehearsal/web/*.ply
pipeline/rehearsal/web/*.sog
pipeline/rehearsal/web/*.splat

# personal Claude Code settings
.claude/settings.local.json

# misc
*.blend1
.DS_Store
# Windows NTFS ADS copy artifacts (Explorer copies into WSL)
*:Zone.Identifier

# python
__pycache__/
*.pyc
