# deps / build
node_modules/
dist/
*.tsbuildinfo

# runtime job state (ADDON.md §6)
jobs/

# heavy source assets live outside git (PLAN.md §8) — git-lfs later if needed
assets_src/
pipeline/rehearsal/web/*.ply
pipeline/rehearsal/web/*.sog
pipeline/rehearsal/web/*.splat

# personal Claude Code settings
.claude/settings.local.json

# misc
*.blend1
.DS_Store
