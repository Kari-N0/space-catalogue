// Landing route entry. The Babylon engine is NOT loaded here and never
// will be on the landing route — it lazy-loads on first 3D interaction
// (PLAN.md §3). The M0 "build <mode> · <year>" debug status line was
// removed in the Phase 2 audit (2026-08-05).

export {};
